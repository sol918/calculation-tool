node_modules/
.next/
.env.local
.env.*.local
*.tsbuildinfo
next-env.d.ts

# DB: de live sqlite-file reist mee zodat de demo-projecten, kengetallen en
# materialen direct beschikbaar zijn bij een fresh clone. De timestamped
# `.backup-*` files blijven lokaal.
data/*.backup-*
data/backups/
data/_*.xlsx

# Local-only: curl cookie jars + Excel-export uit test-scripts.
cookies*.txt
temp_backups/
